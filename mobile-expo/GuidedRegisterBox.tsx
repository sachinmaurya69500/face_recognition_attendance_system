// The mobile registration screen uses the shared guided implementation.
// Its validation requests use the same backend detector, five pose rules,
// landmark alignment, and server-side embedding checks as web registration.
export {default} from './GuidedRegister';
